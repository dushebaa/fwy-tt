import styled from "styled-components";

export const Wrapper = styled.div`
    display: flex;
    flex-direction: column;
    gap: 6px;

    & > label {
        text-align: left;
    }

    & > span {
        color: #646cff;
    }
`